# Finally Using Svelte
The new STEAMachines Website written in Svelte! This project uses pnpm as the package manager as evident in `pnpm-workspace.yaml` and `pnpm-lock.yaml`.

# FOSSness
You can use our `"code"` freely, but prior notification to `STEAMachines_42@outlook.com` or `STEAMMachines42@gmail.com` is required.